<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <%include include/head.ejs %>
</head>
<body>
<div class="showDateArea">
  <p><%= title %></p>
  <h4>这里展示数据库中已录入的数据</h4>
  <% listDate.forEach(function(list){ %>
  <ul>
    <li><span>编号：</span><span><%= list.SortID %></span></li>
    <li><span>姓名：</span><span><%= list.Name %></span></li>
    <li><span>性别：</span><span><%= list.Sex %></span></li>
    <li><span>地址：</span><span><%= list.Address %></span></li>
  </ul>
  <% }) %>
</div>

<hr />

<div class="showArea">
  <h4>采用 <%= title %>测试MongoDB数据的写入、修改、删除、回显等</h4>
    <div><span>编号：</span><input class="sortID" type="test" name="sortID" placeholder="编号"/></div>
    <div><span>姓名：</span><input class="name" type="test" name="name" placeholder="姓名"/></div>
    <div><span>性别：</span><input class="sex" type="test" name="sex" placeholder="性别"/></div>
    <div><span>地址：</span><input class="address" type="test" name="address" placeholder="地址"/></div>
    <button type="button" class="updateDate">提交</button>
</div>


<script type="text/javascript">
  var tempModel = {
    SortID:"",
    Name:"",
    Sex:"",
    Address:"",
    timeDate:"",
  };
  $(function(){
    $(".updateDate").click(function(){
      tempModel.SortID = $(".sortID").val();
      tempModel.Name = $(".name").val();
      tempModel.Sex = $(".sex").val();
      tempModel.Address = $(".address").val();
      tempModel.timeDate = Date.now();
      $.post("/mongo/create",tempModel,function(data){
        location.reload();
      });
    })
  })
</script>
</body>
</html>
